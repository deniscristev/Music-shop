@model NavbarModel
<div class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">MUSICSHOP</a>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-left">
                <li><a href="/Home/Index">Главная</a></li>
                <li><a href="@Url.Action("Abouts","About")">О нас</a></li>
                <li><a href="@Url.Action("Index","Shop")">Каталог</a></li>
                <li class="dropdown">
                    <a id="drop1" href="#" class="dropdown-toggle" data-toggle="dropdown">
                        Категории
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="@Url.Action("Guitar","Shop")">Гитары</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Violin","Shop")">Скрипки</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Keyboard","Shop")">Клавишные</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Wind","Shop")">Духовые</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Accessories","Shop")">Аксессуары</a></li>
                    </ul>
                </li>
                <li>
                    <div class="d7">
                        <form>
                            <input type="text" placeholder="Поиск" text_color="white">
                            <a href="@Url.Action("Search","Shop")"class="glyphicon glyphicon-search" aria-hidden="true" style="color: white;"></a>
                        </form>
                    </div>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <a>@Model.User.Email</a>
                    }


                    @if (User.Identity.IsAuthenticated)
                    {
                        if (Model.User.Role == "admin")
                        {
                        <li><a href="@Url.Action("Index", "Admin")">Панель админа</a></li>
                    }
                    else
                    {
                        <li><a href="@Url.Action("Cart","Shop")">Корзина</a></li>
                    }
                    <li><a href="@Url.Action("Logoff", "Account")">Выйти</a></li>
                }
                else
                {
                    <li><a href="@Url.Action("Login", "Account")">Войти</a></li>
                    <li><a href="@Url.Action("Register", "Account")">Создать аккаунт</a></li>
                }

                </ul>
            </div>
            <!--/.nav-collapse -->
        </div>
    </div>
